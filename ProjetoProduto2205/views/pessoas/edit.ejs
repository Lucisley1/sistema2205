<%- include("../layouts/header", {title: 'Atualização de Pessoas'}) %>

<%- include("../layouts/menu") %>

<section class="section">
    <div class="container">
        <div class="columns">
            <div class="column is-four-fifths">
              <h1 class="title is-4">Atualização de Pessoas</h1>
            </div>
            <div class="column is-one-fifth">
              <div class="buttons is-pulled-right">
                <a href="/pessoas" class="button is-small">Voltar</a>
              </div>
            </div>
          </div>
   
      <form action="/pessoas/<%= pessoa.id %>?_method=put" method="POST">
        <div class="field">
          <label class="label">nome</label>
          <div class="control">
            <input class="input" type="text" name="nome" value="<%= pessoa.nome %>" required>
          </div>
        </div>
  
        <div class="field">
          <label class="label">Rua</label>
          <div class="control">
            <input class="input" type="text" name="rua" value="<%= pessoa.endereco.rua %>" step="0.01" required>
          </div>
        </div>

        <div class="field">
            <label class="label">Cidade</label>
            <div class="control">
              <input class="input" type="text" name="cidade" value="<%= pessoa.endereco.cidade %>" step="0.01" required>
            </div>
          </div>
  
        <div class="field">
          <div class="control">
            <button type="submit" class="button is-success">Atualizar</button>
          </div>
        </div>
      </form>
    </div>
  </section>
  
  <%- include("../layouts/footer") %>